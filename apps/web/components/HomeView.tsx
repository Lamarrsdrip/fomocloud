"use client";
import {ArrowDownToLine,TrendingUp,Users,WalletCards} from "lucide-react";import {money} from "../lib/api";import {timeAgo} from "../lib/format";import {TokenAvatar} from "./TokenAvatar";
export default function HomeView({d,activity,setView,openToken,onFund}:{d:any;activity:any;brain:any[];brainDegraded:boolean;setView:(v:any)=>void;openToken:(s:{chain:string;mint:string})=>void;onFund:()=>void}){const s=d?.summary||{},wallet=(activity?.events||[]).filter((e:any)=>e.type==="WALLET_ACTIVITY"&&e.data?.action==="BUY").slice(0,8);return <><section className="home-hero"><div><span>TOTAL VALUE</span><h2>{money(s.accountValueUsd)}</h2></div><div className="home-hero-pnl"><span>TOTAL PNL</span><b>{money(s.netPnlUsd)}</b></div></section><div className="quick-actions-row"><button onClick={onFund}><ArrowDownToLine/><span>Fund</span></button><button onClick={()=>setView("discover")}><TrendingUp/><span>Live buys</span></button><button onClick={()=>setView("traders")}><Users/><span>Traders</span></button><button onClick={()=>setView("positions")}><WalletCards/><span>Wallet</span></button></div><section className="app-card"><div className="card-title"><div><span>HOW MEMECLOUD WORKS</span><h2>Follow the wallets. Research the token. Trade fast.</h2></div></div><p style={{fontSize:12,color:"#8a8fa0"}}>Admin lists proven meme traders. MemeCloud watches their real swaps, alerts you immediately, researches every bought token, and can trade through Global Brain or your own selected traders.</p><button className="action-primary" onClick={()=>setView("traders")}>Choose traders</button></section><section className="app-card"><div className="card-title"><div><span>LIVE</span><h2>Latest verified buys</h2></div></div>{wallet.map((e:any)=><div className="feed-item tap" key={e.id} onClick={()=>openToken({chain:e.data.chain,mint:e.data.mint})}><TokenAvatar symbol={e.data.symbol||e.data.name}/><div><b>{e.title}</b><small>{e.data.marketCapUsd?`${money(e.data.marketCapUsd)} MC · `:""}{timeAgo(e.createdAt)}</small></div></div>)}{!wallet.length&&<div className="pnl-empty">Waiting for the next verified swap from a tracked trader.</div>}</section></>}
