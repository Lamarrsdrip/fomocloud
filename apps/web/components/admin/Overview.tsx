import {money} from "../../lib/api";import {WalletCards,Users,Activity,Settings2} from "lucide-react";import {Metric} from "./Metric";
export function Overview({d}:{d:any}){const m=d.metrics||{},u=m.users||{},t=m.trading||{},s=m.smartTraders||{},e=m.engine||{};return <><section className="owner-hero"><div><span>MEMECLOUD OWNER</span><h2>Curate wallets. Watch real swaps. Let users copy.</h2><p>The platform is wallet-first now. Add the meme traders you trust; everything starts from their verified on-chain swaps.</p></div></section><section className="admin-quick-grid"><button onClick={()=>document.querySelector<HTMLButtonElement>('button[data-admin-target="traders"]')?.click()}><Users/><div><b>Tracked traders</b><small>Add or remove Admin-curated wallets</small></div></button><button onClick={()=>document.querySelector<HTMLButtonElement>('button[data-admin-target="trades"]')?.click()}><WalletCards/><div><b>Live trades</b><small>User execution activity</small></div></button><button onClick={()=>document.querySelector<HTMLButtonElement>('button[data-admin-target="health"]')?.click()}><Activity/><div><b>Health</b><small>Listener and execution health</small></div></button><button onClick={()=>document.querySelector<HTMLButtonElement>('button[data-admin-target="config"]')?.click()}><Settings2/><div><b>Settings</b><small>Providers and trading configuration</small></div></button></section><section className="admin-kpi-row"><Metric label="Users" value={u.registered}/><Metric label="Auto trade users" value={u.autoCopyEnabled}/><Metric label="Tracked traders" value={s.platform}/><Metric label="Signals today" value={e.signalsToday}/></section><section className="app-card"><div className="card-title"><div><span>REAL EXECUTION</span><h2>Trading</h2></div></div><div className="control-list"><div><span>Open positions</span><b>{t.openPositions??0}</b></div><div><span>Orders today</span><b>{t.ordersToday??0}</b></div><div><span>Allocated cash</span><b>{money(t.allocatedCashUsd??0)}</b></div></div></section></>}
