diff --git a/dist/index.cjs b/dist/index.cjs
index bde0210eb131db06ccca30a5c466c7252a03af50..2ddc73409e1afc682b6b4fc766dcf336209747b6 100644
--- a/dist/index.cjs
+++ b/dist/index.cjs
@@ -163,6 +163,9 @@ var HEIF = {
     while (currentOffset < ipcoBox.offset + ipcoBox.size) {
       const ispeBox = findBox(input, "ispe", currentOffset);
       if (!ispeBox) break;
+      if (ispeBox.size < 20 || ispeBox.offset + ispeBox.size <= currentOffset || ispeBox.offset + ispeBox.size > ipcoBox.offset + ipcoBox.size) {
+        throw new TypeError("Invalid HEIF image property box size");
+      }
       const rawWidth = readUInt32BE(input, ispeBox.offset + 12);
       const rawHeight = readUInt32BE(input, ispeBox.offset + 16);
       const clapBox = findBox(input, "clap", currentOffset);
@@ -253,6 +256,9 @@ var ICNS = {
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
       const imageHeader = readImageHeader(input, imageOffset);
+      if (imageHeader[1] < SIZE_HEADER || imageOffset + imageHeader[1] > inputLength || imageOffset + imageHeader[1] > fileLength) {
+        throw new TypeError("Invalid ICNS entry length");
+      }
       const imageSize2 = getImageSize2(imageHeader[0]);
       images.push(imageSize2);
       imageOffset += imageHeader[1];
@@ -487,6 +493,9 @@ function extractPartialStreams(input) {
   while (offset < input.length) {
     const jxlpBox = findBox(input, "jxlp", offset);
     if (!jxlpBox) break;
+    if (jxlpBox.size < 12 || jxlpBox.offset + jxlpBox.size <= offset) {
+      throw new TypeError("Invalid JXL partial stream box size");
+    }
     partialStreams.push(
       input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size)
     );
diff --git a/dist/index.mjs b/dist/index.mjs
index 22c17afad4e406eba013fff7858366cb5f8e3ba4..22ed5cee6e601a1c9c937608225e8e37a4bab28f 100644
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -159,6 +159,9 @@ var HEIF = {
     while (currentOffset < ipcoBox.offset + ipcoBox.size) {
       const ispeBox = findBox(input, "ispe", currentOffset);
       if (!ispeBox) break;
+      if (ispeBox.size < 20 || ispeBox.offset + ispeBox.size <= currentOffset || ispeBox.offset + ispeBox.size > ipcoBox.offset + ipcoBox.size) {
+        throw new TypeError("Invalid HEIF image property box size");
+      }
       const rawWidth = readUInt32BE(input, ispeBox.offset + 12);
       const rawHeight = readUInt32BE(input, ispeBox.offset + 16);
       const clapBox = findBox(input, "clap", currentOffset);
@@ -249,6 +252,9 @@ var ICNS = {
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
       const imageHeader = readImageHeader(input, imageOffset);
+      if (imageHeader[1] < SIZE_HEADER || imageOffset + imageHeader[1] > inputLength || imageOffset + imageHeader[1] > fileLength) {
+        throw new TypeError("Invalid ICNS entry length");
+      }
       const imageSize2 = getImageSize2(imageHeader[0]);
       images.push(imageSize2);
       imageOffset += imageHeader[1];
@@ -483,6 +489,9 @@ function extractPartialStreams(input) {
   while (offset < input.length) {
     const jxlpBox = findBox(input, "jxlp", offset);
     if (!jxlpBox) break;
+    if (jxlpBox.size < 12 || jxlpBox.offset + jxlpBox.size <= offset) {
+      throw new TypeError("Invalid JXL partial stream box size");
+    }
     partialStreams.push(
       input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size)
     );
diff --git a/dist/types/heif.cjs b/dist/types/heif.cjs
index 1f490788ea654ea9896060a5c0253d81e348af92..31607a7602d0afefd274ce9b7d836dec05c3008f 100644
--- a/dist/types/heif.cjs
+++ b/dist/types/heif.cjs
@@ -59,6 +59,9 @@ var HEIF = {
     while (currentOffset < ipcoBox.offset + ipcoBox.size) {
       const ispeBox = findBox(input, "ispe", currentOffset);
       if (!ispeBox) break;
+      if (ispeBox.size < 20 || ispeBox.offset + ispeBox.size <= currentOffset || ispeBox.offset + ispeBox.size > ipcoBox.offset + ipcoBox.size) {
+        throw new TypeError("Invalid HEIF image property box size");
+      }
       const rawWidth = readUInt32BE(input, ispeBox.offset + 12);
       const rawHeight = readUInt32BE(input, ispeBox.offset + 16);
       const clapBox = findBox(input, "clap", currentOffset);
diff --git a/dist/types/heif.mjs b/dist/types/heif.mjs
index 01330919e01b3c034a96a7b4b1dbf5a7f658bb2d..2ec2c37c66321a2f46dc9f655afc9faecfac092a 100644
--- a/dist/types/heif.mjs
+++ b/dist/types/heif.mjs
@@ -57,6 +57,9 @@ var HEIF = {
     while (currentOffset < ipcoBox.offset + ipcoBox.size) {
       const ispeBox = findBox(input, "ispe", currentOffset);
       if (!ispeBox) break;
+      if (ispeBox.size < 20 || ispeBox.offset + ispeBox.size <= currentOffset || ispeBox.offset + ispeBox.size > ipcoBox.offset + ipcoBox.size) {
+        throw new TypeError("Invalid HEIF image property box size");
+      }
       const rawWidth = readUInt32BE(input, ispeBox.offset + 12);
       const rawHeight = readUInt32BE(input, ispeBox.offset + 16);
       const clapBox = findBox(input, "clap", currentOffset);
diff --git a/dist/types/icns.cjs b/dist/types/icns.cjs
index 32c8d370dcf80a10f14a6deebfdbe04d3c70585c..fa788536d7d7b129df2d685d8ac49d6c7e565747 100644
--- a/dist/types/icns.cjs
+++ b/dist/types/icns.cjs
@@ -72,6 +72,9 @@ var ICNS = {
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
       const imageHeader = readImageHeader(input, imageOffset);
+      if (imageHeader[1] < SIZE_HEADER || imageOffset + imageHeader[1] > inputLength || imageOffset + imageHeader[1] > fileLength) {
+        throw new TypeError("Invalid ICNS entry length");
+      }
       const imageSize = getImageSize(imageHeader[0]);
       images.push(imageSize);
       imageOffset += imageHeader[1];
diff --git a/dist/types/icns.mjs b/dist/types/icns.mjs
index 8710027bc5890a79d05a7fdb87de3018e556729e..432b27fbb12a6cd9d32ba5652d3a3266a7a66fa7 100644
--- a/dist/types/icns.mjs
+++ b/dist/types/icns.mjs
@@ -70,6 +70,9 @@ var ICNS = {
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
       const imageHeader = readImageHeader(input, imageOffset);
+      if (imageHeader[1] < SIZE_HEADER || imageOffset + imageHeader[1] > inputLength || imageOffset + imageHeader[1] > fileLength) {
+        throw new TypeError("Invalid ICNS entry length");
+      }
       const imageSize = getImageSize(imageHeader[0]);
       images.push(imageSize);
       imageOffset += imageHeader[1];
diff --git a/dist/types/jxl.cjs b/dist/types/jxl.cjs
index a12095be85c7a8fd25763895a47c46b4292ebd9c..c9e32ea935427ad3f76bc2a4d4203417ea52a95e 100644
--- a/dist/types/jxl.cjs
+++ b/dist/types/jxl.cjs
@@ -116,6 +116,9 @@ function extractPartialStreams(input) {
   while (offset < input.length) {
     const jxlpBox = findBox(input, "jxlp", offset);
     if (!jxlpBox) break;
+    if (jxlpBox.size < 12 || jxlpBox.offset + jxlpBox.size <= offset) {
+      throw new TypeError("Invalid JXL partial stream box size");
+    }
     partialStreams.push(
       input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size)
     );
diff --git a/dist/types/jxl.mjs b/dist/types/jxl.mjs
index 49b0c4cac6abb366378e5feed745f16981b441c0..105073dac6a78fa48cab767ac6466109da12d1bc 100644
--- a/dist/types/jxl.mjs
+++ b/dist/types/jxl.mjs
@@ -114,6 +114,9 @@ function extractPartialStreams(input) {
   while (offset < input.length) {
     const jxlpBox = findBox(input, "jxlp", offset);
     if (!jxlpBox) break;
+    if (jxlpBox.size < 12 || jxlpBox.offset + jxlpBox.size <= offset) {
+      throw new TypeError("Invalid JXL partial stream box size");
+    }
     partialStreams.push(
       input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size)
     );
