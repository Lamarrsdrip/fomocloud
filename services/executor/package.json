{
  "name": "@fomocloud/executor",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo executor lint",
    "test": "echo executor tests"
  },
  "dependencies": {
    "@fomocloud/db": "workspace:*",
    "@fomocloud/shared": "workspace:*",
    "@fomocloud/execution": "workspace:*",
    "bullmq": "^5.58.1",
    "ioredis": "^5.7.0"
  },
  "devDependencies": {
    "tsx": "^4.20.4",
    "typescript": "^5.9.2",
    "@types/node": "^22.18.0"
  }
}
